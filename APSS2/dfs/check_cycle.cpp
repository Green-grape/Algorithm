#include <iostream>
#include <vector>

using namespace std;

vector<vector<int>> adj; //adj[i] i->인 정점 배열
vector<bool> finished; //finished[i] dfs[i]가 끝났는지 확인

int main(){

}

void initialize(const int vertexCount){
    adj=vector<vector<int>>(vertexCount, vector<int>());
    finished=vector<i   
}